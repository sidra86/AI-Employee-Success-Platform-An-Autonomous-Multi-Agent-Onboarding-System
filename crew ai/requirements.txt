# Core web stack (required for fresh clone)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
sqlalchemy>=2.0.25
python-multipart>=0.0.9
jinja2>=3.1.3
python-dotenv>=1.0.0
requests>=2.31.0

# Optional LLM (system falls back to mock_crew if unavailable / no API key)
openai>=1.12.0

# RAG / documents (ChromaDB preferred; in-memory vector fallback if unavailable)
chromadb>=0.4.22
pypdf>=4.0.0
